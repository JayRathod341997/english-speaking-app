# Production backend URL (used by `npm run build`). Include the /api suffix.
# Leave unset for local dev — Vite proxies /api to localhost:8000.
VITE_API_BASE_URL=https://your-backend.onrender.com/api
